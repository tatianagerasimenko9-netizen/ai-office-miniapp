# 17. Токени / LLM

Прод-рахунки Anthropic **не надані**. Оцінка за кодом і чатом.

## Де найбільші витрати (PROBABLE)

1. **Ручний тікер** → повний Лев + tools (до 5) + іноді Олеся. 514 аналізів за період; XAU 73, BTC 64.  
2. **Марічка** домашка: 419 повідомлень × ~663 символи output + довгий контекст W/D.  
3. **Сканер раз на годину в KZ:** ланцюг 5–6 LLM на один `best` (макс, марічка, дарина, марко, лев).  
4. **WATCHING reanalyze** (травень XAU) — історичний спам викликів.  
5. `LEV_RULE` + Gerchik kernel + історія сигналів у промпті — великий **input** щоразу.

## Caps

`max_tokens` lev 1200 / інші 1000; сканер 500–900; send trim ~3800.  
Тому лонг-сценарій «не дописав» (20.09) — **ймовірно cap**, не Telegram 4096.

## Дубль контексту

Одні й ті самі REST (ATR, sweep, regime) рахуються в `fetch_edge_score` **і** можуть знову викликатись tools Лева. Probability і edge ділять джерела.

## Slash як економія

Користувач **не** використовує `/ask` `/desk` у цьому експорті (0 slash). Пише `Btc` / `Ake` → найдорожчий шлях.

## Ціль 2.0

Event/state у БД; LLM лише на CONFIRM/SIGNAL і `/why`. Не кликати 6 агентів на SKIP.
