# Зміни

## Формат запису

- Дата/час (Kyiv):
- Що змінено:
- Чому:
- Очікуваний ефект:

---

## Останні важливі оновлення (сьогодні до ~15:00)

- Дата/час (Kyiv): 2026-04-26 (до ~15:00)
- Що змінено:
  - зібрано і синхронізовано єдину пам'ять по правилах/стану;
  - зафіксовано актуальний режим `PUMPDUMP_SIGNAL_ONLY=false`;
  - підтверджено простий формат комунікації і мінімум шуму.
- Чому:
  - після втрати живлення треба швидко відновлювати контекст без плутанини.
- Очікуваний ефект:
  - нова сесія стартує швидко, без втрати рішень і без суперечностей у налаштуваннях.

---

## Оновлення 27.04.2026 (після нічного інциденту)

- Дата/час (Kyiv): 2026-04-27 ~13:00-14:00
- Що змінено:
  - виправлено критичний баг: після `🚫 ПРОПУСТИ` угода більше не відкривається (додано ранній `return` перед `open_trade` у двох гілках);
  - прибрано рядок `📏 Простір` з тексту сигналів;
  - оновлено заголовок сигналів:
    - LONG: `🟢🟢 LONG 🟢🟢 ... · #N`;
    - SHORT: `🔴🔴 SHORT 🔴🔴 ... · #N`;
    - PREPUMP: `🟡🟡 ПРЕПАМП 🟡🟡`;
    - PREDUMP: `🟠🟠 ПРЕДАМП 🟠🟠`;
  - виправлено облік закриття угод:
    - додано поля `close_reason` і `outcome` (`WIN` / `LOSS` / `BE`) у `record_closed_trade`;
    - виправлено формулу PnL у закритті по стопу для LONG/SHORT (тепер знак напрямний, не `-abs(...)`);
    - Telegram-повідомлення закриття тепер показує фінальний фінрезультат (`🟢/🟡/🔴`) і `Причина: SL hard/SL tech`;
  - у `daily_report` додано підсумковий рядок:
    - `🏆 WIN: X · 💔 LOSS: Y · 🟡 BE: Z`;
  - CHATGPT-фільтр переведено в hard-block:
    - поза сесією дає `🚫 ПРОПУСТИ` (не half-size);
  - TP3-логіка для скальпу:
    - прибрано жорсткий cap TP3;
    - додано попередження `⚠️ далека ціль`, якщо TP3 > 50%.
- Чому:
  - вночі 26-27.04 були серійні входи поза сесією, а також некоректне відображення прибуткового закриття як збитку;
  - потрібна чесна статистика і зрозумілий фінрезультат у повідомленнях.
- Очікуваний ефект:
  - `SKIP` реально блокує угоду;
  - прибуткові trailing/SL закриття більше не показуються як червоний мінус;
  - підсумкова денна статистика прозоріша;
  - позасесійний ризик зменшується за рахунок hard-block.

## Що зафіксовано як правила (оновлено 27.04.2026)

- після кожної зміни обов'язково вказувати адресу коду бота: `C:\Users\Pentagon\Downloads\2scanner_bot_1.py`;
- автозбереження змін кожні 10 хв без нагадування;
- після важливих правок запускати `UPDATE_DATE.bat` для автооновлення дати папки пам'яті.

## Відкриті пункти (після останніх правок)

- контролювати прод-синхронізацію: перенос правок з робочої копії у `C:\Users\Pentagon\Downloads\2scanner_bot_1.py`;
- у наступних сесіях перевіряти, що всі нові кейси закриття пишуться з коректним `outcome`;
- за потреби додатково посилити фільтр ліквідності для нових/тонких монет.

---

## Оновлення 28.04.2026 (SMC модуль + аудит)

- Дата/час (Kyiv): 2026-04-28 ~10:00-10:15
- Що змінено:
  - створено окремий конспект-аудит: `Смарт Мані Концепт.md` (детальний розбір розділів методички і відповідності коду);
  - додано окремий SMC-модуль сигналів `scan_sym_smc(...)` (без заміни існуючого `scan_sym`);
  - додано SMC-параметри в `CONFIG`:
    - `SMC_ENABLED`,
    - `SMC_MAX_SIGNALS`,
    - `SMC_MIN_SCORE`,
    - `SMC_TF`;
  - додано окремий лічильник `stats["smc_signals"]`;
  - інтегровано SMC-скан у `run_scan(...)` паралельно з ICT-сканом;
  - додано SMC-логи діагностики: `SMC_SKIP` і `SMC_SIGNAL`;
  - додано для SMC:
    - KZ-вікна за методичкою (Asia/London/NY),
    - Judas-перевірку в перші 30 хв KZ,
    - блок NY Lunch 16:00-17:00,
    - score-модель SMC 0-20 і поріг входу.
- Чому:
  - потрібно протестувати SMC окремо від ICT, не ламаючи живий пайплайн;
  - потрібен детальний аудит по методичці перед повною уніфікацією.
- Очікуваний ефект:
  - SMC-сигнали підуть окремим каналом логіки;
  - можна порівнювати результат SMC vs ICT на живих даних;
  - менше ризику регресії в базовому ICT-сканері.

## Що не зроблено (зафіксовано, щоб не забути)

- зміни поки внесені у робочу копію `C:\Users\Pentagon\Desktop\БОТ_ПАМЯТЬ_26.04.2026\file-1.py.py`, ще не перенесені у основний файл `C:\Users\Pentagon\Downloads\2scanner_bot_1.py`;
- повна уніфікація KZ/Judas/NY Lunch в основному ICT-контурі (`scan_sym`) не виконана (за рішенням тестувати SMC окремо);
- не проведено підсумковий A/B звіт SMC vs ICT за 1-2 живі торгові дні;
- не оформлено окремий rollback-тогл для швидкого вимкнення SMC через runtime-команду (є лише `SMC_ENABLED` у конфігу).

## Оновлення 28.04.2026 (P0: regime/correlation/loss-streak)

- Дата/час (Kyiv): 2026-04-28 ~10:45-10:55
- Що змінено:
  - додано P0.1 детектор ринку `detect_market_regime(...)` з класами `CHOP` / `TREND` / `IMPULSE`;
  - інтегровано regime-gate у `scan_sym(...)` та `scan_sym_smc(...)`:
    - `CHOP` -> skip сигналу;
    - `IMPULSE` -> потрібен підвищений `score` (через `MARKET_IMPULSE_MIN_SCORE`);
  - додано відображення режиму ринку в тексті сигналу (`fmt_signal`: рядок `🧭 Режим ринку: ...`);
  - додано P0.2 жорсткий directional cap:
    - новий конфіг `MAX_CORRELATED_SAME_DIR`,
    - helper `corr_same_side_allowed(...)`,
    - перевірка перед усіма `open_trade(...)` (ICT + pump/dump + SMC);
  - додано P0.3 cooldown по серії збитків:
    - `LOSS_STREAK_MAX`, `LOSS_STREAK_PAUSE_MIN`,
    - runtime-поля `loss_streak`, `loss_streak_pause_until`,
    - блок сканування в `scan_sym(...)` і `scan_sym_smc(...)` під час паузи;
  - у `trade_watcher(...)` додано логіку оновлення loss-streak:
    - серія інкрементується лише при реальному збитку по SL,
    - при TP/BE скидається,
    - при досягненні порога надсилається Telegram-пауза.
- Чому:
  - зменшення overtrading у флєті, зниження концентрації позицій в один бік, і автоматична пауза після серії поганих входів.
- Очікуваний ефект:
  - менше входів у CHOP, вища якість сигналів в IMPULSE;
  - нижчий ризик серійного directional drawdown;
  - стабільніша денна крива після 2+ поспіль збиткових закриттів.

---

## Оновлення 30.04.2026 (легенда повідомлень)

- Дата/час (Kyiv): 2026-04-30 ~15:00
- Що змінено:
  - додано файл `ЛЕГЕНДА_ПОВІДОМЛЕНЬ.md` з таблицею: тип повідомлення -> іконка -> дія трейдера.
- Чому:
  - зафіксувати прості правила інтерпретації повідомлень для ручного трейдингу.
- Очікуваний ефект:
  - швидке розуміння, що робити після кожного алерту, без зайвих пояснень.

---

## Оновлення 01.05.2026 (дисциплінарні фільтри + стратегія "Ціль дня")

- Дата/час (Kyiv): 2026-05-01
- Що змінено:
  - додано дисциплінарні параметри в `CONFIG`:
    - `DISCIPLINE_MODE_ENABLED`,
    - `DISCIPLINE_SCALP_SESSION_ONLY`,
    - `DISCIPLINE_ALLOW_ASIA`,
    - `DISCIPLINE_MAX_SAME_DIR`;
  - додано helper `discipline_session_ok(...)` для контролю сесійного контексту;
  - у `scan_sym(...)` додано:
    - блок позасесійних SCALP-входів (за дисциплінарним правилом),
    - hard-блок серійних входів в один бік через `DISCIPLINE_MAX_SAME_DIR`;
  - AI-оцінка сесійного контексту (`in_session_now`) тепер використовує дисциплінарне правило;
  - створено окремий файл стратегії:
    - `СТРАТЕГІЯ_ЦІЛЬ_ДНЯ_01.05.2026.md`
    - (план: стабілізація -> P0 тест -> TARGET MODE -> звітність).
- Чому:
  - зменшити хаотичні входи, стабілізувати WR і денний ризик перед увімкненням `Ціль дня`.
- Очікуваний ефект:
  - менше шумових входів поза clean-zone;
  - менше корельованих серій в один бік;
  - більш стабільна денна крива до етапу TARGET MODE.
  - блок `Ціль дня` працює ізольовано;
  - ICT/SMC/PREPUMP/PREDUMP не блокує при `TARGET_BLOCK_ALL_SIGNALS=False`.

---

## Оновлення 01.05.2026 (PREPUMP/PREDUMP: не лише ніч)

- Дата/час (Kyiv): 2026-05-01
- Що змінено:
  - у `CONFIG` додано:
    - `PREPUMP_REQUIRE_NIGHT_ONLY=False` (дозвіл PREPUMP/PREDUMP не тільки вночі),
    - `PREPUMP_GUARDS_ONLY_IN_NIGHT_WINDOW=True` (нічні risk-guard-и тільки для нічного вікна);
  - у `detect_night_prepump_setup(...)` змінено умову доступу:
    - жорсткий нічний фільтр тепер вмикається лише якщо `PREPUMP_REQUIRE_NIGHT_ONLY=True`;
  - у `scan_sym(...)` нічні обмеження (`killswitch`, ліміти, expectancy gate) для класу PREPUMP/PREDUMP застосовуються тільки коли активний нічний guard;
  - створено окрему детальну стратегію:
    - `СТРАТЕГІЯ_PREPUMP_PREDUMP_01.05.2026.md`.
- Чому:
  - за 3 дні PREPUMP/PREDUMP майже не генерувалися; вузьке "тільки ніч" вікно обмежувало вибірку і корисність модуля.
- Очікуваний ефект:
  - збільшення частоти валідних PREPUMP/PREDUMP сигналів;
  - збереження контрольованого ризику у нічні години;
  - краща статистична оцінка модуля на реальній вибірці.

---

## Оновлення 01.05.2026 (стратегічні бонуси в score)

- Дата/час (Kyiv): 2026-05-01
- Що змінено:
  - у `CONFIG` додано:
    - `BTC_MSB_BONUS_ENABLED`, `BTC_MSB_30M_PCT`,
    - `ASIAN_SWEEP_QUALITY_BONUS_ENABLED`, `ASIAN_SWEEP_MIN_ATR_MULT`,
    - `POSITIONAL_TF_BONUS_ENABLED`;
  - у `scan_sym(...)` додано 3 бонусні перевірки:
    - `BTC_MSB_CONFIRM +2` (BTC 30m імпульс у бік сигналу),
    - `ASIAN_SWEEP_QUALITY +2` (широкий Asian range + sweep у бік входу),
    - `POSITIONAL_TF +1` (таймфрейм `1h`/`4h`);
  - у `fmt_signal(...)` додано показ бонусів:
    - рядок `⚡ Бонуси: ...` (лише якщо бонуси є);
  - заголовки PREPUMP/PREDUMP змінено з "Нічне вікно" на нейтральний "Поведінковий сетап".
- Чому:
  - посилити відбір контекстних сигналів без зміни базової архітектури.
- Очікуваний ефект:
  - краща пріоритезація сигналів при однаковому базовому сетапі;
  - прозорість для трейдера: видно, за що саме додано перевагу.

---

## Оновлення 01.05.2026 (daily-звіт по бонусах)

- Дата/час (Kyiv): 2026-05-01
- Що змінено:
  - у `open_trade(...)` додано збереження `bonus_tags` у стані активної угоди;
  - у `record_closed_trade(...)` `bonus_tags` пишеться в `closed_trades_log`;
  - у `daily_report(...)` додано блок:
    - `⚡ Бонуси (по закритих угодах)`,
    - загалом з бонусами: count + WR + USD,
    - окремо `BTC_MSB_CONFIRM`, `ASIAN_SWEEP_QUALITY`, `POSITIONAL_TF`.
- Чому:
  - потрібна перевірка, чи реально бонуси покращують результат, а не просто піднімають score.
- Очікуваний ефект:
  - прозорий щоденний контроль ефективності бонусного шару;
  - швидке рішення: лишати/посилювати/вимикати окремі бонуси.

---

## Оновлення 01.05.2026 (PREPUMP/PREDUMP anti-silence тюнінг)

- Дата/час (Kyiv): 2026-05-01
- Що змінено:
  - помʼякшено PREPUMP/PREDUMP-пороги в `CONFIG`:
    - `NIGHT_PREPUMP_ONLY_MOVERS: True -> False`,
    - `NIGHT_PREPUMP_VOL_EXPAND_X: 1.30 -> 1.15`,
    - `NIGHT_PREPUMP_LEVEL_TOL_PCT: 1.20 -> 1.80`.
- Чому:
  - модуль міг мовчати не через баг, а через надмірно суворі критерії відбору.
- Очікуваний ефект:
  - більше валідних PREPUMP/PREDUMP сигналів у спокійні періоди;
  - краща збірка статистики без вимкнення risk-ядра.
